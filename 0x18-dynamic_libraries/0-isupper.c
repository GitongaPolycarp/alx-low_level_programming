#include "main.h"
/**
 * _isupper - function chesks uppercase letters
 * @c: char
 *
 * Return: if uppercase 1, else 0
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'E')
		return (1);

	return (0);
}
